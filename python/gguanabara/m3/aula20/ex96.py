def area(l, c):
    total = l * c
    print(f'A área do total é de {total} M²...')

largura = float(input('Digite a largura: '))
comprimento = float(input('Digite o comprimento: '))

area(largura, comprimento)